#pragma once

#include "controller/AppController.h" 

// -- 类定义 ----------------------------------------------------------------------
class MenuFactory {
public:
    static void build(AppController* app);  
};